# Keep line endings sane on both sides: a .cmd with bare LF endings can confuse
# cmd.exe, and a shell script with CRLF endings will not run at all.
*.sh   text eol=lf
*.cmd  text eol=crlf
*.ps1  text eol=crlf
*.vbs  text eol=crlf
